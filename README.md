# .github

Community health and Sponsor configuration defaults for the [@synthesisengineering](https://github.com/synthesisengineering) organization.

The `FUNDING.yml` in this repo applies to every public repository under the organization that does not have its own `.github/FUNDING.yml`. Individual repos can override.
